node_modules
.next
out

# variáveis de ambiente — NUNCA commitar
.env
.env.*
!.env.example

.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# planilha original com dados reais da loja
MOTOBOYS.xlsx

# Claude Code
.claude/

# brand source assets — gerados via HiggsField, mantemos só as variantes otimizadas
public/brand/*-original-*.png

# Playwright MCP artefatos
.playwright-mcp/
