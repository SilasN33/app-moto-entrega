// Easing padrão da landing (expo-out suave) — tupla tipada porque o
// framer-motion não aceita number[] genérico como cubic-bezier.
export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];
